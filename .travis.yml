language: android

before_install: cd HelloWorld
jdk: oraclejdk7

env:
  global:
    - TERM=dumb

#script: gradle build
install:
  - TERM=dumb ./gradlew assemble

script:
  - TERM=dumb ./gradlew check
